
The Nautilus application development team was working on a git repository /opt/apps.git which is cloned under /usr/src/kodekloudrepos directory present on Storage server in Stratos DC. The team want to setup a hook on this repository, please find below more details:



Merge the feature branch into the master branch`, but before pushing your changes complete below point.

Create a post-update hook in this git repository so that whenever any changes are pushed to the master branch, it creates a release tag with name release-2023-06-15, where 2023-06-15 is supposed to be the current date. For example if today is 20th June, 2023 then the release tag must be release-2023-06-20. Make sure you test the hook at least once and create a release tag for today's release.

Finally remember to push your changes.
Note: Perform this task using the natasha user, and ensure the repository or existing directory permissions are not altered.


######## solution down ######

ssh natasha@172.16.238.15

Password: Bl@kW

cd /opt/apps.git/hooks

mv post-update.sample post-update

vi post-update 

        #!/usr/bin/bash
        for ref in "$@"; 
        do
            if [ "$ref" = "refs/heads/master" ]; 
            then
                TAG_NAME="release-$(TZ="America/Chicago" date +%F)"
                git tag "$TAG_NAME"
                echo "Remote: Tag created $TAG_NAME"
            fi
        done
        exec git update-server-info

cd /usr/src/kodekloudrepos/apps/

git checkout master

git merge feature

git push

cd /opt/apps.git

git tag -> Submit the case





##### IF NOT ABLE TO SEE TAG #####

#### then let's again try to create master branch push operation ###

cd /usr/src/kodekloudrepos/apps/

echo "testing first" >> test.txt

git add .

git config --global user.name "natasha"

git commit -am "adding first test"

git push

cd /opt/apps.git

git tag





    1  cd /usr/src/kodekloudrepos/demo/
    2  git checkout mas
    3  git checkout master
    4  git merge feature
    5  vi /opt/demo.git/hooks/post-update.sample 
    6  git push
    7  git tag
    8  vi /opt/demo.git/hooks/post-update.sample 
    9  git tag /opt/demo.git/
   10  git tag /opt/demo.git
   11  cd /opt/demo.git/
   12  git tag
   13  cd /usr/src/kodekloudrepos/demo/
   14  echo "test" >> test.txt
   15  git add .
   16  git commit -am "adding test"
   17  git config --global user.email "ststor01.stratos.xfusioncorp.com"
   18  git commit -am "adding test"
   19  git config --global user.name "natasha"
   20  git commit -am "adding test"
   21  git push
   22  vi /opt/demo.git/hooks/post-update.sample 
   23  mv /opt/demo.git/hooks/post-update.sample /opt/demo.git/hooks/post-update
   24  echo "test 2" >> test2.txt
   25  git add .
   26  git commit -am "adding second test"
   27  git push
   28  history





   TAG_NAME="release-$(TZ="America/Chicago" date +%F)"
                git tag -a "$TAG_NAME" -m "Released at: $TAGNAME"
                echo "Remote: Tag created $TAG_NAME"
                echo "---------------------------------------------"